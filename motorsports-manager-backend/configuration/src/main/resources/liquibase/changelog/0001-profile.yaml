databaseChangeLog:
  - changeSet:
      id: 1
      author: fin.kingma
      comment: created new table for profiles
      changes:
        - createTable:
            tableName: profiles
            columns:
              - column:
                  name: name
                  type: varchar(50)
                  constraints:
                    primaryKey: true
                    nullable: false
