type Mutation {
    createTeam(team: TeamInput): Team
}
type Query {
    teamByName(name: String): Team
    getAllTeams: [Team!]
}

type Team {
    id: ID!
    name: String
    slogan: String
}

input TeamInput {
    name: String
    slogan: String
}